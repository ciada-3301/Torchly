[project]
name = "torchly"
version = "0.1.0"
description = "Lightweight neural network framework"
authors = [{name = "Arkadyuti Maiti"}]
dependencies = ["numpy"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"